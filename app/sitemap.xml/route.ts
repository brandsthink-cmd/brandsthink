import {site,services,locations,industries,articles,caseStudies} from '@/content/site';
export function GET(){const paths=['','services','industries','case-studies','clients','insights','about','contact','privacy',...services.map(x=>x.slug),...locations.map(x=>x.slug),...industries.map(x=>'industries/'+x.slug),...articles.map(x=>'insights/'+x.slug),...caseStudies.filter(x=>x.approved).map(x=>'case-studies/'+x.slug)];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(p=>`<url><loc>${site.origin}/${p?p+'/':''}</loc></url>`).join('')}</urlset>`,{headers:{'Content-Type':'application/xml; charset=utf-8','Cache-Control':'public, max-age=3600'}})}
